server:
  port: 8080
  error:
    include-message: always

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/search
    password: sanchit4628e...
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

